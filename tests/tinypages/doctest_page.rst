Doctest Page
============

Bare ``>>>`` blocks, recorded only when ``autocodelink_doctest_blocks`` is on.
Both sections below use ``pkg.thing``, so the "Used In" entry for this page has
to pick one -- the later ``Examples`` section wins over ``Notes``.

Notes
-----

>>> import pkg
>>> pkg.thing()
1

Examples
--------

>>> import pkg
>>> pkg.thing()
1
