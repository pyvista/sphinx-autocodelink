"""
Plotting another thing
=======================

Also uses the fixture's documented ``pkg.thing``, from a second example --
enough for Sphinx-Gallery's ``parallel=True`` mode to hand examples to
separate joblib worker processes.
"""

import pkg

pkg.thing()
