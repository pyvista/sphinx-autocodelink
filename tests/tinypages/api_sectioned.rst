Sectioned API
=============

Two documented objects, but only one carries example code -- so every code block on the
page shares a single ``Examples`` section, the same shape as an enum whose members have
no examples of their own.

.. autofunction:: pkg.sectioned_example

.. autofunction:: pkg.no_examples_here
